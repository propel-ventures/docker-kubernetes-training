[req]
default_bits           = 4096
prompt                 = no
default_md             = sha256
req_extensions         = req_ext
distinguished_name     = req_distinguished_name

[req_distinguished_name]
countryName            = AU
stateOrProvinceName    = Victoria
organizationName       = Propel
organizationalUnitName = Kubernetes
commonName             = Session10
emailAddress           = kevin.osborne@propelventures.ai

[req_ext]
subjectAltName         = @alt_names

[alt_names]
DNS.1                  = localhost
DNS.2                  = local
IP.1                   = 127.0.0.1
